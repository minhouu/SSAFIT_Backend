<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<!-- 아직 구상중 -->
<mapper namespace="com.ssafy.ssafit.model.dao.ExerciseRecordDao">

	<!-- create -->
	<insert id="insertRecord" parameterType="Record">
		<selectKey keyProperty="record_id" order="AFTER" resultType="int">
            SELECT LAST_INSERT_ID()
        </selectKey>
		INSERT INTO user (record_date, user_seq, trainner_seq)
		VALUES
		(record_date, user_seq, trainner_seq);
	
	
	</insert>
	
	<!-- read -->
	
	
	<!-- update -->
	
	
	<!-- delete -->



</mapper>